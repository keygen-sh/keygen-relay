# wrong public key should fail verification
exec sh -c 'key=$(cat "$1"); relay add --file "$2" --key "$key" --public-key deadbeef' _ $E2E_EXPORT_DIR/license.key $E2E_EXPORT_DIR/license.lic
stderr 'license verification failed'

# correct public key should succeed
exec sh -c 'key=$(cat "$1"); pub=$(cat "$2"); relay add --file "$3" --key "$key" --public-key "$pub"' _ $E2E_EXPORT_DIR/license.key $E2E_EXPORT_DIR/public.key $E2E_EXPORT_DIR/license.lic
stdout 'license added successfully'
